import 'package:flutter/material.dart';

class NotificationsTabContent extends StatelessWidget {
  const NotificationsTabContent({super.key});

  @override
  Widget build(BuildContext context) {
    // Replace with your actual screen content for "Notifications"
    // This might involve fetching and displaying MatchSuggestions or other alerts.
    return const Center(
      child: Text(
        'Notifications / Alerts Screen',
        style: TextStyle(fontSize: 24, fontWeight: FontWeight.bold),
      ),
    );
  }
}
